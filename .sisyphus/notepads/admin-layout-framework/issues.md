- `frontend/tsconfig.app.json` excludes `src/**/__tests__/*`, so the AdminLayout test needed an explicit `env.d.ts` reference to resolve Vue SFC imports during diagnostics.
- Manual QA login on the current branch is most likely failing because `frontend/src/views/LoginView.vue:12` defaults the backend field to `window.location.origin`, so submitting from `http://127.0.0.1:5174/login` probes `http://127.0.0.1:5174/api/v1/protected`; with MSW disabled by the local-only `frontend/.env` value `VITE_USE_MOCKS=false` and `frontend/src/main.ts:13-18`, that request bypasses `frontend/src/mocks/handlers.ts:13-25` and instead goes through the Vite proxy in `frontend/vite.config.ts:22-30` to the real backend at `http://localhost:8000`, where it returns the observed 500. `git blame` shows the login default came from the original login implementation and the mock toggle came from an earlier frontend config commit, so this appears to be a pre-existing dev-environment/config interaction rather than a regression from current Tasks 3/4. Smallest safe fix path later: either use the actual API origin in the login form during manual QA or re-enable MSW for frontend-only QA by removing/overriding the local `VITE_USE_MOCKS=false` setting; do not change both at once.
- F3 theme review found that `frontend/src/layouts/AdminLayout.vue` depends on `--theme-bg-primary`, `--theme-bg-secondary`, `--theme-bg-elevated`, and `--theme-text-primary`, but those variables are not defined anywhere in `frontend/src` (`grep` only found usages in `AdminLayout.vue`). In live Playwright inspection, `data-theme` switched correctly and Naive UI menu text colors changed between parchment and starry-night, yet the header/sidebar shell surfaces stayed effectively unthemed because the custom layout variables resolved to empty values. This is a real visual blocker for final approval, distinct from the unrelated login/backend dev-environment issue above.

- F1 final verification on 2026-03-18 failed in two buckets: (1) `npm run lint` is blocked by `frontend/src/layouts/__tests__/AdminLayout.test.ts:1` using `/// <reference path="../../../env.d.ts" />`, which appears directly tied to this plan because the same workaround was previously recorded for the AdminLayout test; (2) `npm run type-check` is blocked by unrelated frontend issues in `frontend/src/theme/parchment.ts:323`, `frontend/src/theme/starry-night.ts:233` (`Dropdown.peers.InternalMenu` no longer matches the Naive UI theme override type) and `frontend/src/api/__tests__/client.test.ts:36` (axios interceptor mock callback types are too loose for the current axios typings). These type-check errors are outside the admin-layout files and should be treated as pre-existing/unrelated unless later evidence shows they landed in the same change wave.

- F1 theme override blocker in `frontend/src/theme/starry-night.ts` and `frontend/src/theme/parchment.ts` was caused by a Naive UI typings change: the installed `DropdownTheme` still exposes dropdown option tokens like `optionTextColor`, `optionColorHover`, `optionColorActive`, `prefixColor`, `suffixColor`, and `dividerColor`, but `Dropdown.peers.InternalMenu` is no longer a valid override key. Smallest safe fix was to remove the unsupported nested `InternalMenu` blocks and map the intended menu text/hover/active/divider colors onto the typed `Dropdown` option-level fields; `npm run type-check` then passed for the frontend.
