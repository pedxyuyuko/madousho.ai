- AdminLayout should use Naive UI shell primitives (`n-layout`, `n-layout-sider`, `n-layout-header`, `n-layout-content`) while styling stays anchored to existing theme CSS variables like `--theme-bg-primary`, `--theme-hover`, and `--color-border`.
- Co-located Vitest tests in this frontend commonly stub Naive UI and child components instead of mounting the whole app shell, which keeps layout behavior assertions stable.

- LanguageSwitcher user-facing labels should be `中文` and `English` while keeping internal locale values as `zh-CN` and `en-US`; tests should assert the visible labels plus the `✓` marker on the active option.

- The admin layout E2E flow is stable when Playwright seeds `madousho_backends` and `madousho_theme` in `page.addInitScript()` before `page.goto('/')`; this avoids the unreliable login API path while still exercising the real router guard, header order (`☀️`/theme, language, backend, logout), sidebar toggle, and logout redirect to `/login`.

- For this repo’s Playwright workflow, the robust spec pattern is to seed localStorage before navigation and wait for app-owned markers like `data-testid="admin-layout"`; keep helpers lean and remove unused top-level constants so `lsp_diagnostics` stays clean without changing the working assertions.

- Final-wave browser QA on 2026-03-18 passed against the live Vite app at `http://127.0.0.1:5173` using seeded `madousho_backends` + `madousho_theme` localStorage. Verified `/` renders `data-testid="admin-layout"`, header action order remains theme → language → backend → logout, sidebar collapse/expand works, theme toggles parchment ↔ starry-night with no console/page errors, logout redirects to `/login`, and screenshots were saved to `.sisyphus/evidence/final-qa/admin-expanded-light.png`, `admin-collapsed-light.png`, `admin-expanded-dark.png`, and `admin-restored-light.png`.
- Minimal F3 fix path for `frontend/src/layouts/AdminLayout.vue`: do not invent missing global shell vars. Instead, keep shared custom vars that already exist (`--color-border`, `--theme-hover`, `--theme-text-muted`) and apply admin layout surfaces/text through full global `[data-theme='...'] .admin-*` selectors using concrete values already present in `starry-night.ts` and `parchment.ts` (for example starry body/card text colors and parchment body/card text colors). When verifying live theme changes, wait past the 350ms global color/background transition in `frontend/src/assets/css/themes.css`; sampling immediately after the toggle can falsely show stale header/sidebar colors.

- `frontend/tsconfig.app.json` already includes `env.d.ts`, so `frontend/src/layouts/__tests__/AdminLayout.test.ts` does not need a local `/// <reference path="../../../env.d.ts" />`; removing that line fixes the triple-slash lint error without affecting Vitest compilation for the AdminLayout test.
- Vue SFC shim declarations in `frontend/env.d.ts` and `frontend/src/components/__tests__/vue-shim.d.ts` can stay lint-clean by using `DefineComponent<Record<string, never>, Record<string, never>, unknown>` instead of `any`; this preserves the `declare module '*.vue'` default-export behavior while satisfying `@typescript-eslint/no-explicit-any` with no new LSP diagnostics.

- `frontend/src/api/__tests__/client.test.ts` should derive mocked axios response interceptor callback types from `Parameters<typeof instance.interceptors.response.use>` instead of using a loose `(value: unknown) => unknown` alias; that keeps the capture wrapper assignable to axios's generic `use` signature without changing test behavior.
