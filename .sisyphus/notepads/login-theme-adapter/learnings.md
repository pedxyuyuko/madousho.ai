# Learnings - Login Theme Adapter

- CSS custom properties can be grouped logically by theme within `[data-theme]` attribute selectors
- The pattern of adding login-specific variables alongside theme variables works well for component styling
- Both starry-night (purple/dark) and parchment (warm/amber) themes now support consistent login styling