# Decisions

- User explicitly approved keeping the status clear `x` affordance (`data-testid="flows-status-clear-button"`) as a reasonable part of the status-select UX; do not remove it just to satisfy a mechanical "exactly four controls" audit reading.
